# Test Configuration Environment Variables
# Copy this file to .env.test and customize as needed

# Test output directories (used by test runner)
TEST_OUTPUT_DIR=./test-results
TEST_TEMP_DIR=/tmp/raft-test-temp
TEST_COVERAGE_DIR=./coverage-reports

# Docker host directories (used by docker-compose.test.yml)
HOST_TEST_RESULTS_DIR=./test-results
HOST_COVERAGE_DIR=./coverage-reports
HOST_TEMP_DIR=./temp

# Container directories (used inside Docker containers)
# These are configured in docker-compose.test.yml and should match volume mounts
# TEST_OUTPUT_DIR=/app/test-results (container path)
# TEST_TEMP_DIR=/tmp/test-temp (container path)
# TEST_COVERAGE_DIR=/app/coverage-reports (container path)

# Example: Custom directories for CI/CD
# TEST_OUTPUT_DIR=/workspace/test-output
# TEST_TEMP_DIR=/workspace/temp
# TEST_COVERAGE_DIR=/workspace/coverage
# HOST_TEST_RESULTS_DIR=/tmp/ci-test-results
# HOST_COVERAGE_DIR=/tmp/ci-coverage
# HOST_TEMP_DIR=/tmp/ci-temp

# Example: Shared network storage
# HOST_TEST_RESULTS_DIR=/shared/project/test-results
# HOST_COVERAGE_DIR=/shared/project/coverage
# HOST_TEMP_DIR=/shared/project/temp